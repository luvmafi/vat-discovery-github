# Initial source matrix

| Source | Expected signal | Phase-0 state | Acquisition / constraints | Decision |
|---|---|---|---|---|
| Company website | VAT label in legal/footer/contact text | Piloted across ~130 distinct companies (Phase 2 rounds 1-3, n=36, plus Phase 10 manual-validation subset, n=100, with 6 overlaps reused not re-queried): **6 real, first-party-confirmed VAT candidates found** (HILLS FAMILY LTD, JTHN LIMITED, GO2 PROPERTY SERVICES LIMITED, G A PLANT AND TOOL HIRE LTD, TTG PORTSMOUTH LIMITED, BULLET BUILDING PRODUCTS LIMITED) — roughly 4.6% overall, 6% on the larger unbiased n=100 draw specifically. Every one capped at TIER_3 by missing authoritative verification, not by weak evidence (one scores 0.96/1.0 on every other signal). Also surfaced 4 hallucinated/unconfirmable search-summary claims (~40% false-claim rate among all VAT-number claims investigated) and found+fixed a checksum-validator sign-error bug that had been misclassifying most real 9755-variant VAT numbers as invalid. Full detail: `docs/findings.md`, `experiments/source_website*.py`, `experiments/manual_validation_*.py` | Robots, terms, domain throttle, cache (not yet exercised — pilot used the operating agent's own fetch tool, not a scaled crawler) | CONDITIONAL GO — real, non-zero, non-trivial signal confirmed (~5%); blocked on an authoritative verifier (HMRC or equivalent), not on discovery itself |
| Public PDFs on company domains | Invoice/terms/footer VAT evidence | Piloted n=8 (`filetype:pdf` query per company, same 8 as website pilot): 0/8 company-specific PDFs surfaced at all. Extraction module (`extraction/pdf.py`, TEXT-layer only, no default OCR) built and unit-tested but never exercised on a real document — none was found. Detail: `docs/findings.md`, `experiments/source_pdf.py` | Download limits; text extraction before OCR — not yet exercised at scale | NO-GO for `filetype:pdf` search alone on this sample; PENDING a more targeted test (e.g. Companies House filed-accounts PDFs) before ruling out the source entirely |
| Search provider | Official-domain and PDF leads | Piloted using agent-mediated search only across 36 companies (no credentialed `SearchProvider` adapter exists). Produced real signal (2 confirmed candidates) but also produced one hallucinated VAT-number claim not present on the actual cited page — search-result *summaries* are not a trustworthy evidence source on their own; every claim in this pilot was independently re-fetched and confirmed before being recorded. | Provider terms/API and query cost unresolved — no real API credentials configured yet; hallucination risk specifically applies to LLM-summarized search results, not to a raw-HTML SearchProvider adapter | PENDING: select/credential a real provider that returns raw result data (not summaries), then reproduce the pilot as an automated, cacheable adapter run |
| Companies House | Identity, status, SIC, address, dates | Ingested real 2026-08-01 snapshot: 5,695,465 rows read, 5,190,464 kept (`CompanyStatus == Active`); 500-company stratified sample generated, all 50 strata represented | Free monthly bulk CSV snapshot, manual download, no formal OGL grant but Companies House states no reuse restriction on this product as statutory public information (unverified against a written licence document) | INCLUDE as population backbone only |
| HMRC Check a UK VAT Number API | Verification name/address after candidate discovery | Untested | Authentication, stated due-diligence purpose, rate/terms | CONDITIONAL verifier only |
| Procurement/spend records | Supplier identifiers/evidence | Untested | Licence, freshness, public-sector scope | PENDING feasibility test |
| Business directories | Possible official-domain lead | Untested | Commercial reuse and provenance risk | PENDING; never sole high-confidence evidence |

No coverage, precision, cost, or commercial-usability conclusion exists yet.
